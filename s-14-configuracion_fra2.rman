rman target /
-- Respaldo de los archivos de parámetros (SPFILE) y Archivo de control
BACKUP SPFILE;
BACKUP CURRENT CONTROLFILE;

-- Respaldo de tipo BackupSet (Incluye datos y archivelogs)
BACKUP DATABASE PLUS ARCHIVELOG;

-- Respaldo de tipo Image Copy
BACKUP AS COPY DATABASE;